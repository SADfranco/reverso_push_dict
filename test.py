from reverso_context_api import Client
import random


#def favwordtest():
      #client = Client("en", "ru", credentials=("shabaevandrew@gmail.com", "Aw253634"))
      #result = list(client.get_favorites())
      #ran = random.choice(result)
      #print(ran)
    #  print(result)
    #  new_line = '\n'
    #  for i in result:
    #      favor = f"{i['source_text']},{i['source_context']}/{i['target_text']} {i['target_context']}"
    #      list_result.append(favor)
    #  print(list_result)
    #  ran = random.choice(list_result)
    #  ran_split = ran.split("/")
    #  return ran_split

#client = Client("en", "ru", credentials=("shabaevandrew@gmail.com", "Aw253634"))
#result = list(client.get_favorites())
#print(result)
#ran = random.choice(result)
#source_text = str(ran['source_text'])
#target_text = str(ran['target_text'])
#source_context = str(ran['source_context'])
#remove_context = source_context[:-1]
#target_context = str(ran['target_context'])
#remove_target = target_context[:-1]
#print(source_text,target_text,source_context,target_context)
#print(source_text,target_text,remove_context,remove_target)
#for i in ran:
#     source_text = str(i['source_text'])
#     source_context = str(i['target_text'])
#     total = f"{source_text}={source_context}"
#     print(total)